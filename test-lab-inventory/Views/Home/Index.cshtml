@{
    ViewBag.Title = "Home Page";
}

<main>
    <section class="row" aria-labelledby="inventoryTitle">
        <h1 id="inventoryTitle">Welcome to the LIMS Inventory Sandbox</h1>
        <p class="lead">
            We are iterating on an AI-assisted workflow for monitoring laboratory stock.
            Jump into the dashboard to see how heuristics highlight potential risks.
        </p>
        <p>@Html.ActionLink("Open the AI Dashboard", "Index", "Inventory", new { area = "" }, new { @class = "btn btn-primary btn-md" })</p>
    </section>

    <div class="row">
        <section class="col-md-4" aria-labelledby="dataLayerTitle">
            <h2 id="dataLayerTitle">1. Data layer</h2>
            <p>Review <code>Data/InventoryRepository.cs</code> to understand how we seed representative inventory records.</p>
        </section>
        <section class="col-md-4" aria-labelledby="insightLayerTitle">
            <h2 id="insightLayerTitle">2. Insight engine</h2>
            <p>Walk through <code>Services/InventoryInsightService.cs</code> to see the heuristics that mimic AI behaviour.</p>
        </section>
        <section class="col-md-4" aria-labelledby="uiLayerTitle">
            <h2 id="uiLayerTitle">3. Presentation</h2>
            <p>The controller and Razor view in <code>Controllers/InventoryController.cs</code> and <code>Views/Inventory/Index.cshtml</code> complete the experience.</p>
        </section>
    </div>
</main>
